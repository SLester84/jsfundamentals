//Variables
let var1;
const var2;
var var3;


//Datatypes
/*
sting 'text'
number 'integer'
boolean 'true or false'
object 'alpha numeric for example' holds multiple datatypes or can be called containers
null  'doesn't = zero but is just empty'
symbol 'no one uses anymore'
undefined 'just hasn't been defined'
*/

//Conditional

let color = 'green'

if (color == 'green'){
    console.log(color);
} else {
    console.log('something');
}
